{
  "name": "surfstar",
  "main": "dist/index.js",
  "version": "0.0.6",
  "type": "module",
  "scripts": {
    "build": "bun build --target=bun --minify index.ts --outdir ./dist && bun run build:types",
    "build:types": "bun run src/build-types.ts"
  },
  "devDependencies": {
    "bun-plugin-dts": "^0.2.1",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "wave-shell": "0.0.10"
  }
}
